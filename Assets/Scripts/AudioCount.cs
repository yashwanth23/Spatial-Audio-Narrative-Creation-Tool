using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioCount : MonoBehaviour
{
    public static int Audio_count;
    // Start is called before the first frame update
    void Start()
    {
        Audio_count = 1;
    }
    
}
